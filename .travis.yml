 language: node_js
 node_js:
   - "6"
install:
  - npm install
script:
  - npm test && npm run make-clean && npm run make-lib && npm run make-dist
deploy:
  provider: npm
  email: radu.helstern@gmail.com
  api_key: $NPM_TOKEN
  skip_cleanup: true
  on:
    tags: true
